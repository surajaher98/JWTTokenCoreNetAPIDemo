{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "JWT": {
    "ValidAudience": "MyValidAudience",
    "ValidIssuer": "MyValidIssuer",
    "SecretKey": "the server key used to sign the JWT token is here, use more than 16 chars"
  }
}
